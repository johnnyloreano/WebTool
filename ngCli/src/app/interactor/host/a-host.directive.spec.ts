import { AHostDirective } from './a-host.directive';

describe('AHostDirective', () => {
  it('should create an instance', () => {
    const directive = new AHostDirective();
    expect(directive).toBeTruthy();
  });
});
